{{#each this}}
<div class="card2 comments">
    <div class="card-body">
        <h5>Commented by {{user.username}} on {{format_date date_created}}</h5>
        <p>{{text}}</p>
    </div> 
</div>
{{each}}